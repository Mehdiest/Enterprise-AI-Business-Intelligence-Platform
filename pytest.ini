[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --ignore=tests/manual
markers =
    asyncio: mark a test as running on the asyncio event loop.

